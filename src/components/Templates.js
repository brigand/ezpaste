import React, {PropTypes} from 'react';

export default
class Templates extends React.Component {
  static propTypes = {
    code: PropTypes.string,
  };

  render() {
    return (
      <div>

      </div>
    );
  }
}
